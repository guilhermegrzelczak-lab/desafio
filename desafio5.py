#armazena a base e a altura do triangulo a ser calculada a area
base = float(input("informe a base do triangulo"))
altura = float(input("inorme a altura do triangulo"))

#calcula a area
area = (base * altura) / 2

#informa o resultado
print(f"A área do triângulo é igual a {area}.")